// use crate::*;

pub fn solve(_input: &str) -> u64 {
    0
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn solve_example() {
        let result = solve(
            "
                "
            .trim(),
        );
        assert_eq!(result, 0);
    }
}
